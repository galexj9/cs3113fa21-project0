#include <stdio.h>
#include "list.h"

/* my linked list implementation */

void push(Node* head, void* data) {
}

Node* get(Node* head, void* data) {
  return head;
}

Node* sort(Node* head, void* data) {
  return head;
}

void print(Node* head) {

}
